{
  "name": "recipes-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:server": "pnpm --filter recipes-server run dev",
    "dev:client": "pnpm --filter recipes-client run dev",
    "build:shared": "pnpm --filter @recipes/shared run build",
    "build": "pnpm run build:shared && pnpm --filter recipes-server run build && pnpm --filter recipes-client run build",
    "typecheck": "pnpm --filter recipes-server run typecheck && pnpm --filter recipes-client run typecheck",
    "lint": "pnpm --filter recipes-server run lint && pnpm --filter recipes-client run lint"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
